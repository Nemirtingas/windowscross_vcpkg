# windowscross_vcpkg

This project will build a Docker Image that can cross-compile for Windows in C/C++ and that supports CMake + vcpkg.

See the different branches and pick the version of MSVC/Windows you need.
